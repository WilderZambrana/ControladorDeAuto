import posFinal from "./App";

const cadena_input = document.querySelector("#cadena-input");
const form = document.querySelector("#controlador-form");

form.addEventListener("submit", (event) => {
  event.preventDefault();

  const cadena = cadena_input.value;

  //const precioInicial = subTotal(cantidad, precio);

  /*document.getElementById('posInicial').innerHTML = ;
  document.getElementById('comandos').innerHTML = ;
  document.getElementById('posFinal').innerHTML = ;*/
});
